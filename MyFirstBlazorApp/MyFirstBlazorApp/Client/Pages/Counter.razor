@page "/counter"

<h1>Counter</h1>
<p>The counter value is: @currentCounter</p>

<button class="btn btn-primary" @onclick="IncrementCounter">Increment counter</button>

@code {
    private int currentCounter = 42;

    private void IncrementCounter()
    {
        currentCounter++;    
    }
}
